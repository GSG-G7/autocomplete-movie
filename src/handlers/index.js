const homeHandler = require('./homeHandler');
const staticsHandler = require('./staticsHandler');
const findHandler = require('./findHandler');
const getMovieDetailsHandler = require('./getMovieDetailsHandler');

module.exports = {
  homeHandler,
  staticsHandler,
  findHandler,
  getMovieDetailsHandler,
};
